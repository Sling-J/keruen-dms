import { moduleName as authModule } from 'src/modules/auth/reducers'

export const AUTH = `${authModule}/credentials/AUTH`
export const LOGOUT = `${authModule}/credentials/LOGOUT`
export const CLEAR_ALL = `${authModule}/credentials/CLEAR_ALL`
