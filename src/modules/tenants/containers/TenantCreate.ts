import { connect } from 'react-redux'

import { TenantCreate } from 'modules/tenants/pages'

const mapStateToProps = () => ({})
const mapDispatchToProps = () => ({})

export default connect(mapStateToProps, mapDispatchToProps)(TenantCreate)
